function setup() {
  createCanvas(400, 200); // Set up the canvas
  textAlign(CENTER, CENTER); // Align text to the center
  textSize(32); // Set text size
}

function draw() {
  background(220); // Light gray background
  fill(0); // Set text color to black
  text("Hello, World from Sana Sukumar!", width / 2, height / 2); // Display text in the center
}
